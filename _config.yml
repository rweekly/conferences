# Permalinks
permalink:           /:title.html

# Setup
title:               Conferences by RWeekly
url:                 http://rweekly.github.io/conferences
paginate:            1
baseurl:             ""
nav-baseurl:         "http://rweekly.github.io/conferences"
ampdir: amp
description: Collect slides and other presentation materials from various R conferences/meetups.
post_max: 396+24

collections:
  conferences:
    output: true

defaults:
  -
    scope:
      path: ""
      type: "conferences"
    values:
      layout: "conf"
# Assets
#
# We specify the directory for Jekyll so we can use @imports.
sass:
  sass_dir:          _sass
  style:            :compressed

gems: [jekyll-paginate]
